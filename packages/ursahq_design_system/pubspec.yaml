name: ursahq_design_system
description: Shared design system for UrsaHQ — dark theme, status indicators, nav components.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
