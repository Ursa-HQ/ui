name: fleet_dashboard
description: Hermes agent fleet monitoring dashboard — seasonal dark theme.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

  ursahq_app_base:
    git:
      url: https://github.com/Ursa-HQ/ui.git
      path: packages/ursahq_app_base
      ref: master

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
