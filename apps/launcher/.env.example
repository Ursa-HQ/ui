# UrsaHQ Launcher — Environment Variables
#
# The launcher currently uses a baked-in service config.
# Future enhancement: point to a JSON endpoint for dynamic service lists.
# LAUNCHER_API_URL=http://your-server-ip:8080/api/services
