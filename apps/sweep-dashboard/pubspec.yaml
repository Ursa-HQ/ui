name: sweep_dashboard
description: UrsaHQ sweep pairs dashboard — live progress, signal results, aggregate stats.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
